// Declare Vars
var inputField, button, output, inputValue;

// Set up my vars

inputField = document.getElementById('userText');
button = document.getElementById('userBtn');
output = document.getElementById('userName');


/*
1.watch for a click of "button"
2.when click happens, I grab value of "inputField"
3.dump that value into my "output" element
*/

button.onclick = function () {

  inputValue = inputField.value;

  output.inner
};
<script type="text/javascript" src="main.js"></script>
